{% extends 'base.html.twig' %}

{% block title %}Phoenix Space Industries{% endblock %}
{% block stylesheets %}<link rel="stylesheet" href="{{ asset('assets/css/style.css') }}">{% endblock %}

{% block li %}
    <li><a href={{ path('solar-map') }}>Map</a></li>
    <li><a href={{ path('destinations') }} >Our Destinations</a></li>

    {% if app.user %}
    <li><a href={{ path('page_account') }}>Account</a></li>
    {% else %}
    <li><a href={{ path('app_login') }}>Login</a></li>
    {% endif %}

{% endblock %}

{% block main %}


    <section class="empty"></section>
    <section class="section1 texts">
        <div class="intro">
            <img src="assets/img/logos/3eme_logo(1).png" alt="" class="resp logo">
            <h1>Welcome to<br>Phoenix Space Industries</h1>
        </div>
        <br>
        <div>
            <div>
                <div class="d-flex space-around">
                    {{ form_start(searchForm) }}
                    <label for="{{ searchForm.departure.vars.id }}">Departure</label>
                    <input type="text" id="{{ searchForm.departure.vars.id }}" value="{{ searchForm.departure.vars.value }}">
                    {% do searchForm.departure.setRendered %}

                    <label for="">Arrival</label>
                    <input type="text" id="{{ searchForm.arrival.vars.id }}" value="{{ searchForm.arrival.vars.value }}">
                    {% do searchForm.arrival.setRendered %}
                    {{ form_end(searchForm) }}
                </div>


            </div>
        </div>
        <div class="travel">
            <div>

            </div>
        </div>
    </section>
    <section class="empty"></section>
    <h2 class="texts">Trending</h2>
    <section class="section2">

        <div class="trending">
            <div>
                <img src="assets/img/Windows.png" alt="" class="resp windows">
            </div>
            <div>
                <img src="assets/img/Windows.png" alt="" class="resp windows">
            </div>
            <div>
                <img src="assets/img/Windows.png" alt="" class="resp windows">
            </div>
        </div>
        <div class="trending">
            <div>
                <img src="assets/img/Windows.png" alt="" class="resp windows">
            </div>
            <div>
                <img src="assets/img/Windows.png" alt="" class="resp windows">
            </div>
            <div>
                <img src="assets/img/Windows.png" alt="" class="resp windows">
            </div>
        </div>
    </section>

<section class="empty"></section>

{% endblock %}
