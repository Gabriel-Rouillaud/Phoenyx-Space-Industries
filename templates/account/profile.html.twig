{% extends 'base.html.twig' %}

{% block title %}My Profile{% endblock %}

{% block li %}
    <li><a href={{ path('solar-map') }}>Map</a></li>
    <li><a href={{ path('page_index') }} >Home</a></li>
    <li><a href={{ path('app_logout') }}>Logout</a></li>
{% endblock %}

{% block main %}

    <div>
        <h1>Your Profile</h1>
    </div>

    <a href="{{ path('page_account') }}"><div>Back</div></a>

<div>
    {{ form_start(form) }}
    {{ form_end(form) }}
</div>

{% endblock %}