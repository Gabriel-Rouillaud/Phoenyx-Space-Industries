{% extends 'base_index.html.twig' %}

{% block title %}Destination{% endblock %}

{% block body %}
    <h1>Destination</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ destination.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ destination.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ destination.description }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ destination.duration ? destination.duration|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ destination.price }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ destination.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('destination_index') }}">back to list</a>

    <a href="{{ path('destination_edit', {'id': destination.id}) }}">edit</a>

    {{ include('destination/_delete_form.html.twig') }}
{% endblock %}
